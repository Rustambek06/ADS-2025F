#include <iostream>
#include <string>
#include <>

int main() {
    int T; 
    std::cin >> T;
    while (T--) {
        int N; 
        std::cin >> N;

        std::string s;
        std::getline(std::cin, s);

        for (char ch: s) {
            if (ch != ' ') {

            }
        }
    }
} 